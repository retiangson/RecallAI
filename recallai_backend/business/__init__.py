"""
Business layer for RecallAI.

This package aligns the structure with the KaiHelper project by grouping
application services under `business.services` (and optionally
`business.interfaces` in the future).

All logic still lives in the original `recallai_backend.services` package.
"""
